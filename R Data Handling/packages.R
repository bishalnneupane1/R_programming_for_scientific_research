install.packages("ggplot2")

# to install more than one packages together
install.packages(c("dplyr", "devtools", "foreign"))

#for update all packages
#check old packages that needs an update
old.packages()

#for update all packages
update.packages()
